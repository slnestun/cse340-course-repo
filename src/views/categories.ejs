<%- include('partials/header') %>
<main>
    <h1><%= title %></h1>
    <p>Here are some of the categories we offer:</p>
    <ul>
        <% categories.forEach(category => { %>
            <li><strong><a href="/category/<%= category.category_id %>"><%= category.name %></a></strong></li>
        <% }); %>
    </ul>
</main>
<%- include('partials/footer') %>
